[tool.poetry]
name = "power"
version = "0.1.0"
description = "Read sensor data real time from energi data service"
authors = ["Øyvind Horne"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.13"
requests = "^2.32.5"
pytest = "^8.4.2"
ruff = "^0.14.2"
pydantic = "^2.12.3"
pandas = "^2.3.3"
matplotlib = "^3.10.7"
seaborn = "^0.13.2"


[tool.poetry.group.dev.dependencies]
ruff = "^0.14.2"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
